# 3d-anatomy currently reads no environment variables — a static Vite
# front-end with all data (models, lexicon, translations) shipped as static
# assets under public/. Kept as a template per the project root interface
# (nqwrc.dots setup/01, "Standard Internal Structure for Every Project
# Folder") for whenever an env var is actually needed.
